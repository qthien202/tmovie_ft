name: app_update
description: OTA update (GitHub release) logic for TMovie apps
publish_to: 'none'
version: 1.0.0

resolution: workspace

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  core:
    path: ../core

  flutter_riverpod: ^2.6.1
  dio: ^5.9.1
  package_info_plus: ^8.0.0
  path_provider: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
